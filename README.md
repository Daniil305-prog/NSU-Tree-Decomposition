# Вычисление древовидной декомпозиции графа (Treewidth Solver)

Данный проект разработан в рамках учебного курса НГУ. Программа реализует 5 различных эвристик для поиска оптимального порядка исключения вершин графа с целью минимизации ширины его древовидной декомпозиции (treewidth).

## Реализованные эвристики

* **Minimum Degree** — жадный выбор вершины с минимальной текущей степенью.
* **Minimum Fill-In** — выбор вершины, минимизирующей количество добавляемых хорд (edges).
* **Maximum Cardinality Search (MCS)** — последовательный выбор вершин с максимальным числом посещенных соседей.
* **Nested Dissection** — алгоритм вложенных сечений на основе поиска сепараторов (разделяй и властвуй).
* **Гибридный алгоритм (ND + MCS)** — комбинация глобального разделения и локального упорядочивания.

## Особенности реализации

* **Встроенный валидатор:** Автоматически проверяет построенное дерево на строгое соответствие трем математическим аксиомам древовидной декомпозиции.
* **Информативный парсер:** Безопасное чтение графов в стандартном формате PACE/DIMACS (`.gr`) с защитой от ошибок в структуре файла и выходом за границы памяти.
* **Оптимизация по памяти:** Использование динамически расширяемых списков смежности на чистом C.

---

## Тестовые данные

В репозитории подготовлен набор тестовых графов различных топологий в формате PACE/DIMACS. Вы можете найти их в папке проекта (файлы с расширением `.gr`):
* `cycle_30.gr` — простой цикл из 30 вершин (идеально для быстрой проверки корректности).
* `grid_5x5.gr` — регулярная решетка размера $5 \times 5$.
* *И другие структуры для комплексного тестирования эффективности эвристик.*

---

## Компиляция и запуск

Для сборки проекта необходим компилятор `gcc`.

### 1. Компиляция
```bash
gcc main.c graph.c heuristics.c tree_decomp.c -o graph_solver.exe